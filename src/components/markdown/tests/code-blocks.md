Inline `code` has back-ticks around it.

```md
Inline `code` has back-ticks around it.
```

Blocks of code are fenced by lines with three back-ticks.

```
// this is some code
const add = (a, b) => a + b
```

    ```
    // this is some code
    const add = (a, b) => a + b
    ```
