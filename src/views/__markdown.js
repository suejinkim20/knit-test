import { Page } from '@components/layout'
import { MarkdownTests } from '@components/markdown/tests'

export const MarkdownView = () => {
  return (
    <Page size="lg" title="Markdown Showcase">
      <MarkdownTests />
    </Page>
  )
}
